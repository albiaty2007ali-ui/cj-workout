# AI_ROADMAP.md

توصية استراتيجية لتطور CJ WORKOUT نحو "AI حقيقي" بدل Templates فقط — مطلوبة صراحة كجزء من
مرحلة Phase 1 (راجع `DATASET_GUIDE.md` للجانب العملي/How-To). هذا المستند رأي فني محدد،
مو استعراض لكل الخيارات الممكنة.

## القيود الملزمة (من CLAUDE.md، ثابتة بكل الخطوات القادمة)

- **Local-first**: بدون استدعاء LLM/API خارجي وقت التشغيل الفعلي بالإنتاج.
- **الأرقام دائمًا من القواعد**: أي AI مستقبلي يصوغ الجملة فقط — ممنوع يحسب سعرة/كمية
  (`nutrition_ai/provider.py`'s الالتزام الحالي، غير قابل للتفاوض).
- Python/Flask، بدون بنية تحتية GPU معروفة اليوم (Render Free Tier، جهاز تطوير Windows).
- لا نرسل بيانات مستخدمين لخدمة خارجية.

## المقارنة

| الخيار | يحتاج تدريب؟ | يحتاج GPU إنتاج؟ | يحل مشكلة "فهم اللهجة"؟ | التوافق مع القيود |
|---|---|---|---|---|
| **Rephrase محلي فقط (Ollama/llama.cpp)** | لا | لا (نموذج صغير على CPU يكفي لصياغة قصيرة) | جزئيًا — يحسّن الصياغة بس القواعد لسا تفهم النية | ✅ الأعلى توافقًا |
| **RAG (Embeddings محلية)** | لا | لا | يحسّن دقة الإجابة عن أسئلة معرفية (تغذية عامة) | ✅ متوافق تمامًا |
| **LoRA/QLoRA على موديل صغير** | نعم (تدريب خفيف) | يحتاج GPU للتدريب، ممكن CPU للاستدلال ببطء | يحسّن فهم اللهجة فعليًا لو الداتا كافية | ⚠️ يحتاج قرار Model + وقت |
| **Fine-tuning كامل** | نعم (ثقيل) | نعم بالتدريب والاستدلال غالبًا | نعم، لو الداتا ضخمة وعالية الجودة | ❌ غير مبرر لمهمة صياغة فقط |

**التوصية**: ابدأ بـ**Hybrid Rules + LLM محلي للصياغة فقط** (موجود أصلاً كعقد بـ`provider.py`،
غير مفعّل)، ثم RAG لو احتجنا دقة أعلى بأسئلة `GENERAL_NUTRITION`. لا مبرر لـFine-tuning
كامل أو حتى LoRA قبل ما نجرب الخيارين الأرخص فعليًا ونكتشف أنهم غير كافيين.

## خارطة الطريق — 6 مراحل

### المرحلة 1: البنية (Architecture) — ✅ منجزة بهذي الجلسة
`AIProvider` قابل للتوسعة (`NullAIProvider`/`OllamaProvider`/`LlamaCppProvider`/
`OpenAICompatibleProvider`)، `NullAIProvider` يبقى الافتراضي بالإنتاج، بنية `knowledge/`،
Schema موحّد، أدوات Import/Validate/Stats، Seed Dataset أولي. صفر تغيير بسلوك الشات.

### المرحلة 2: Seed Dataset
البيانات الحالية بـ`knowledge/` (161 مثال يغطون كل الـIntents الحقيقية) نقطة بداية —
تنمو تدريجيًا كل ما احتجنا تغطية أوسع، بنفس الأداة والـSchema، بدون إعادة هيكلة.

### المرحلة 3: Local LLM (Rephrase فقط) — أرخص خطوة حقيقية تالية
فعّل `OllamaProvider.rephrase()` محليًا (`AI_PROVIDER=ollama` بالتطوير أول شي، موديل عربي/
متعدد اللغات صغير مثل Qwen2.5 أو أي موديل يدعم العربية بشكل جيد). هذا **فعليًا** نفس نمط
"Hybrid Rules + LLM" — القواعد تحدد النية والأرقام كما هي اليوم بالضبط، والموديل يعيد صياغة
الجملة النهائية فقط لتصير أطبع. صفر تغيير على `orchestrator.py`، صفر خطر على منطق الحساب.
هذا يستحق التجربة بالتطوير أولاً قبل أي قرار بالإنتاج.

### المرحلة 4: RAG / Knowledge Base
لما يكبر `knowledge/` (خصوصًا `nutrition/` و`faq/`)، حوّله لـEmbeddings محلية
(مثلاً `sentence-transformers` + فهرس متجهات بسيط) لدعم إجابات `GENERAL_NUTRITION`/الأسئلة
المعرفية بمصدر حقيقي بدل اعتماد الموديل على "معرفته العامة" — يبقى Local-first بالكامل،
بدون أي بيانات تطلع من الجهاز.

### المرحلة 5: Fine-tuning (LoRA/QLoRA) — فقط لو ثبت إنه ضروري فعلاً
لا مبرر له لمهمة صياغة بدون أرقام. يستحق التفكير فيه فقط لو المرحلتين 3 و4 أظهرتا فجوة
حقيقية بفهم اللهجة العراقية تحديدًا ما يحلها Prompting/RAG — عندها يحتاج قرار Model أساسي
حقيقي + ميزانية GPU حقيقية للتدريب (حتى LoRA)، قرار منفصل يحتاج نقاش عند الوصول له فعليًا.

### المرحلة 6: توليد Dataset ضخم — مربوطة صراحة بوجود مستهلك حقيقي
لا نبدأ توليد مئات الآلاف/الملايين من الأمثلة قبل ما يكون عندنا موديل حقيقي (من مرحلة 3
أو 5) يستهلك هالحجم من البيانات فعليًا. توليد بدون مستهلك = هدر وقت وتخزين بدون فايدة —
هذا بالضبط الدرس اللي بدأت منه هذي الخطة كلها.
